"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const glob_1 = __importDefault(require("glob"));
const getPackageJson = (filename) => {
    try {
        return JSON.parse(fs.readFileSync(filename, { encoding: "utf-8" }));
    }
    catch (error) {
        console.error(error);
        return undefined;
    }
};
exports.findPackageJson = (baseDir, ignore = ["**/node_modules/**"]) => {
    const searchPatternString = path.join(baseDir, "**", "package.json");
    return glob_1.default.sync(searchPatternString, { ignore }).reduce((found, packageJsonPath) => {
        const pkg = getPackageJson(packageJsonPath);
        if (pkg) {
            found[packageJsonPath] = {
                data: pkg,
            };
        }
        return found;
    }, {});
};
